/**
 * Finalize cross-note links. In a full implementation this would rewrite
 * Notion page mentions and relations into Obsidian wikilinks using a registry.
 */
export async function finalizeLinks(): Promise<void> {
  // No-op stub.
}
