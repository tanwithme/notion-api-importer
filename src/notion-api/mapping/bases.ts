/**
 * Create a minimal Base YAML definition. Full mapping should copy views,
 * filters and sorts from Notion databases.
 */
export function createBaseYaml(): string {
  return "# Base file stub";
}
