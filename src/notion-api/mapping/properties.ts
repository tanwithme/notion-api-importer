/**
 * Map Notion properties to a generic record. In a full implementation this
 * would translate select, multi-select, relations, and formulas.
 */
export function mapProperties(): Record<string, any> {
  return {};
}
